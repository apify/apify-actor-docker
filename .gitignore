.idea
npm-debug.log
node_modules
.DS_Store

# Temporary Firefox certificates copied to image folders during build
*/firefox-certs/

# Package-manager caches staged into image folders during build (built on demand in CI)
*/package-caches/

# Individual certificate files (we track the zip archive instead)
certificates/certs/
