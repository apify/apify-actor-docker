name: Test Inputs

on:
  repository_dispatch:
    types: [ build-beta ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Print Input
        run: |
          echo "apify: ${{ github.event.client_payload.apify_version }}"
          echo "puppeteer: ${{ github.event.client_payload.puppeteer_version }}"
